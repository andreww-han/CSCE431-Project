<%= form_with(model: like) do |form| %>
  <% if like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(like.errors.count, "error") %> prohibited this like from being saved:</h2>

      <ul>
        <% like.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <% @member = Member.find(@member_id) %>
  <%= form.hidden.field :member_id, :value => @member.id %>
  <% @post = Post.find(@post_id) %>
  <%= form.hidden.field :post_id, :value => @post.id %>

  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>
